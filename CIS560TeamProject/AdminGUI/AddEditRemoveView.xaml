<UserControl x:Class="AdminGUI.AddEditRemoveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminGUI"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Width" Value="400"/>
                <Setter Property="Height" Value="125"/>
                <Setter Property="FontSize" Value="70"/>
                <Setter Property="Margin" Value="50"/>
                <Style.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="20"/>
                    </Style>
                </Style.Resources>
            </Style>
        </Grid.Resources>
        <TextBlock FontSize="75" VerticalAlignment="Top" HorizontalAlignment="Center" x:Name="textTitle"/>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Click="FunctionSelected" Content="Add"/>
            <Button Click="FunctionSelected" Content="Edit"/>
            <Button Click="FunctionSelected" Content="Remove"/>
        </StackPanel>
    </Grid>
</UserControl>
